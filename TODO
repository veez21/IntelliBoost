Todo:
=> Add x86 support
=> Create profiles in UI (Gaming, performance, etc) [Ensure that it's on-the-fly]
=> Execution of scripts on-the-fly using Crossbreeder
=> Add a few scripts to have UI in terminal shell
=> Add kernel tweaks
=> Add governer tweaks (merge govtuner sources, using UI)
=> Forced fast charging
=> Chmod775 specifically
=> GPS Config selection in UI
=> Finalise update-binary
=> init.d enabler (unapck boot.img then repack)
=> Add a loop to disable logging after the 5th boot to insignificantly reduce boot speed and save storage
